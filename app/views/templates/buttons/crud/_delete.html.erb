<%= button_to object, :class => "btn btn-danger btn-block", method: :delete, data: { confirm: 'Are you sure?' }, :disabled => disabled_condition do %>
	<span class="glyphicon glyphicon-trash"></span> Delete
<% end %>