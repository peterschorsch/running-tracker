<%= button_to object, :class => "btn btn-danger btn-block", method: :delete, data: { confirm: 'Are you sure?' }, :disabled => current_user.is_viewer? do %>
	<span class="glyphicon glyphicon-trash"></span> Delete
<% end %>