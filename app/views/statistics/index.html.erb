<div class="container">
    <div class="row">
		<div class="col-md-4 text-center">
			<h2><%= format_month(Date.current) %></h2>
			<hr />
			<p><%= concat_run_totals_line_one(@monthly_stats) %></p>
			<p><%= concat_run_totals_line_two(@monthly_stats) %></p>
    	</div>
		<div class="col-md-4 text-center">
			<h2><%= Date.today.year %></h2>
			<hr />
			<p><%= concat_run_totals_line_one(@yearly_stats) %></p>
			<p><%= concat_run_totals_line_two(@yearly_stats) %></p>
		</div>
		<div class="col-md-4 text-center">
			<h2>All-Time</h2>
			<hr />
			<p><%= concat_run_totals_line_one(@all_time_stats) %></p>
			<p><%= concat_run_totals_line_two(@all_time_stats) %></p>
		</div>
	</div>
</div>