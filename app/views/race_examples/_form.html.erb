<%= form_with(model: race_example, local: true) do |form| %>
  <% if race_example.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(race_example.errors.count, "error") %> prohibited this race_example from being saved:</h2>

      <ul>
      <% race_example.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
